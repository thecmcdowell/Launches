import React from 'react'
import { View, Text, ScrollView, StyleSheet, Image, ActivityIndicator, Linking, Button } from 'r

export default class LaunchDetail extends React.PureComponent {
    static navigationOptions = {
        title: 'Detail'
    };
}